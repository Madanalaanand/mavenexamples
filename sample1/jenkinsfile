node('jdk11-mvn3.8.4') {
    stage('git') {
       git branch: 'main', url: 'https://github.com/Madanalaanand/mavenexamples.git'
    }
    stage('build') {
        sh 'mvn clean package'
    }
    stage('archive'){
        archive 'target/*.jar'
    }
}